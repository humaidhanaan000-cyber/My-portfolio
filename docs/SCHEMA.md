# Database schema

> Generated from `src/lib/db/schema.ts` — do not edit by hand. Regenerate with `npm run docs:schema` after changing the schema (and add a migration).

**42 tables · 637 columns.** All money is stored as integer cents (`amount_cents`, `net_cents`, …); no floating point arithmetic is used for financial values.

## Conventions

| Convention | Detail |
| --- | --- |
| Primary keys | `uuid` generated by the database (`default random uuid`) |
| Money | integer cents, never `float`/`numeric(2)`; currency stored alongside the amount where relevant |
| Timestamps | `timestamptz` (`timestamp with time zone`), always UTC |
| Tenant scope | every business table carries `workspace_id` with a foreign key to `workspaces` |
| Soft delete | long-lived entities (users, workspaces, projects, opportunities) use `deleted_at` instead of hard deletes |
| Demo separation | tables that can hold sample data carry a `demo` boolean; demo rows are excluded from real totals by default |
| Audit | `audit_logs` is append-only and records the actor, action, entity, before/after values and IP |

## Tables

| Table | Area | Columns |
| --- | --- | --- |
| [`users`](#users) | identity | 15 |
| [`workspaces`](#workspaces) | identity | 9 |
| [`memberships`](#memberships) | identity | 6 |
| [`profiles`](#profiles) | identity | 27 |
| [`sessions`](#sessions) | identity | 10 |
| [`auth_tokens`](#auth-tokens) | identity | 7 |
| [`audit_logs`](#audit-logs) | identity | 13 |
| [`plans`](#plans) | billing & settings | 15 |
| [`subscriptions`](#subscriptions) | billing & settings | 11 |
| [`payment_events`](#payment-events) | billing & settings | 10 |
| [`settings`](#settings) | billing & settings | 6 |
| [`agents`](#agents) | agent runtime | 19 |
| [`agent_runs`](#agent-runs) | agent runtime | 18 |
| [`agent_memory`](#agent-memory) | agent runtime | 12 |
| [`memory_summaries`](#memory-summaries) | agent runtime | 9 |
| [`sources`](#sources) | opportunity engine | 22 |
| [`opportunities`](#opportunities) | opportunity engine | 29 |
| [`opportunity_scores`](#opportunity-scores) | opportunity engine | 24 |
| [`strategies`](#strategies) | opportunity engine | 25 |
| [`projects`](#projects) | projects | 26 |
| [`tasks`](#tasks) | projects | 22 |
| [`project_events`](#project-events) | projects | 8 |
| [`workflows`](#workflows) | workflows | 16 |
| [`workflow_runs`](#workflow-runs) | workflows | 13 |
| [`approvals`](#approvals) | approvals | 24 |
| [`content_assets`](#content-assets) | content assets | 24 |
| [`revenue_transactions`](#revenue-transactions) | money & guardrails | 19 |
| [`expenses`](#expenses) | money & guardrails | 16 |
| [`budgets`](#budgets) | money & guardrails | 10 |
| [`budget_ledger`](#budget-ledger) | money & guardrails | 13 |
| [`analytics_events`](#analytics-events) | analytics & operations | 12 |
| [`notifications`](#notifications) | analytics & operations | 14 |
| [`alerts`](#alerts) | analytics & operations | 15 |
| [`api_usage`](#api-usage) | analytics & operations | 16 |
| [`system_logs`](#system-logs) | analytics & operations | 10 |
| [`jobs`](#jobs) | jobs & scheduler | 22 |
| [`schedules`](#schedules) | jobs & scheduler | 17 |
| [`worker_heartbeats`](#worker-heartbeats) | jobs & scheduler | 11 |
| [`reports`](#reports) | jobs & scheduler | 11 |
| [`email_outbox`](#email-outbox) | jobs & scheduler | 11 |
| [`storage_objects`](#storage-objects) | jobs & scheduler | 12 |
| [`opportunity_decisions`](#opportunity-decisions) | jobs & scheduler | 8 |

## Areas

- **identity** — 7 table(s): `users`, `workspaces`, `memberships`, `profiles`, `sessions`, `auth_tokens`, `audit_logs`
- **billing & settings** — 4 table(s): `plans`, `subscriptions`, `payment_events`, `settings`
- **agent runtime** — 4 table(s): `agents`, `agent_runs`, `agent_memory`, `memory_summaries`
- **opportunity engine** — 4 table(s): `sources`, `opportunities`, `opportunity_scores`, `strategies`
- **projects** — 3 table(s): `projects`, `tasks`, `project_events`
- **workflows** — 2 table(s): `workflows`, `workflow_runs`
- **approvals** — 1 table(s): `approvals`
- **content assets** — 1 table(s): `content_assets`
- **money & guardrails** — 4 table(s): `revenue_transactions`, `expenses`, `budgets`, `budget_ledger`
- **analytics & operations** — 5 table(s): `analytics_events`, `notifications`, `alerts`, `api_usage`, `system_logs`
- **jobs & scheduler** — 7 table(s): `jobs`, `schedules`, `worker_heartbeats`, `reports`, `email_outbox`, `storage_objects`, `opportunity_decisions`

### users

Drizzle export: `users`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, not null, default random uuid |
| `email` | text | not null |
| `password_hash` | text | not null |
| `name` | text | not null, default 'user' |
| `role` | text | not null, default 'user' |
| `status` | text | not null, default 'active' |
| `email_verified_at` | timestamptz | — |
| `last_login_at` | timestamptz | not null, default 0 |
| `failed_login_count` | integer | not null, default 0 |
| `locked_until` | timestamptz | — |
| `onboarded_at` | timestamptz | not null, default 'UTC' |
| `timezone` | text | not null, default 'UTC' |
| `createdAt` | now | — |
| `updatedAt` | updated | — |
| `deleted_at` | timestamptz | — |

### workspaces

Drizzle export: `workspaces`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, not null, default random uuid |
| `name` | text | not null |
| `slug` | text | not null |
| `owner_id` | uuid | not null |
| `plan_key` | text | not null, default 'free' |
| `demo_mode` | boolean | not null, default false |
| `createdAt` | now | — |
| `updatedAt` | updated | — |
| `deleted_at` | timestamptz | — |

### memberships

Drizzle export: `memberships`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `workspace_id` | uuid | not null |
| `user_id` | uuid | not null |
| `role` | text | not null, default 'owner' |
| `createdAt` | now | — |
| `updatedAt` | updated | — |

### profiles

Drizzle export: `profiles`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `workspace_id` | uuid | not null |
| `user_id` | uuid | not null |
| `legal_name` | text | not null, default 'US' |
| `country` | text | not null, default 'US' |
| `currency` | text | not null, array, default 'USD' |
| `interests` | text | not null, array, default [] |
| `skills` | text | not null, array, default [] |
| `industries` | text | not null, array, default [] |
| `business_models` | text | not null, array, default [] |
| `monetization_preferences` | text | not null, array, default [] |
| `risk_tolerance` | text | not null, default 'balanced' |
| `automation_level` | text | not null, default 'approval_required' |
| `daily_budget_cents` | integer | not null, default 500 |
| `monthly_budget_cents` | integer | not null, default 5000 |
| `max_project_budget_cents` | integer | not null, default 20000 |
| `per_agent_daily_limit_cents` | integer | not null, default 2000 |
| `score_threshold` | integer | not null, default 70 |
| `min_time_to_revenue_days` | integer | not null, default 0 |
| `max_operating_cost_cents_month` | integer | not null, default 10000 |
| `notify_email` | boolean | not null, default true |
| `notify_browser` | boolean | not null, default true |
| `notify_telegram` | boolean | not null, default false |
| `telegram_chat_id` | text | not null, default 'UTC' |
| `timezone` | text | not null, default 'UTC' |
| `createdAt` | now | — |
| `updatedAt` | updated | — |

### sessions

Drizzle export: `sessions`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `user_id` | uuid | not null |
| `workspace_id` | uuid | not null |
| `token_hash` | text | not null |
| `user_agent` | text | — |
| `ip` | text | not null |
| `expires_at` | timestamptz | not null |
| `revoked_at` | timestamptz | not null, default now() |
| `last_seen_at` | timestamptz | not null, default now() |
| `createdAt` | now | — |

### auth_tokens

Drizzle export: `authTokens`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `user_id` | uuid | not null |
| `type` | text | not null |
| `token_hash` | text | not null |
| `expires_at` | timestamptz | not null |
| `used_at` | timestamptz | — |
| `createdAt` | now | — |

### audit_logs

Drizzle export: `auditLogs`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid, references workspaces.id, on delete cascade |
| `workspace_id` | uuid | references workspaces.id, on delete cascade |
| `user_id` | uuid | not null, default 'user', references users.id, on delete set null |
| `actor_type` | text | not null, default 'user' |
| `actor_key` | text | not null |
| `action` | text | not null |
| `entity_type` | text | — |
| `entity_id` | text | — |
| `before` | jsonb | — |
| `after` | jsonb | — |
| `ip` | text | — |
| `user_agent` | text | — |
| `createdAt` | now | — |

### plans

Drizzle export: `plans`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, not null, default random uuid |
| `key` | text | not null |
| `name` | text | not null, default '' |
| `tagline` | text | not null, default '' |
| `price_monthly_cents` | integer | not null, default 0 |
| `price_yearly_cents` | integer | not null, default 0 |
| `currency` | text | not null, default 'USD' |
| `features` | jsonb | not null, default [] |
| `limits` | jsonb | — |
| `provider_price_id` | text | not null, default true |
| `is_public` | boolean | not null, default true |
| `is_active` | boolean | not null, default true |
| `sort_order` | integer | not null, default 0 |
| `createdAt` | now | — |
| `updatedAt` | updated | — |

### subscriptions

Drizzle export: `subscriptions`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `workspace_id` | uuid | not null |
| `plan_key` | text | not null, default 'free' |
| `status` | text | not null, default 'active' |
| `provider` | text | not null, default 'none' |
| `provider_customer_id` | text | — |
| `provider_subscription_id` | text | — |
| `current_period_end` | timestamptz | not null, default false |
| `cancel_at_period_end` | boolean | not null, default false |
| `createdAt` | now | — |
| `updatedAt` | updated | — |

### payment_events

Drizzle export: `paymentEvents`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid, references workspaces.id, on delete set null |
| `workspace_id` | uuid | not null, references workspaces.id, on delete set null |
| `provider` | text | not null |
| `type` | text | not null |
| `provider_event_id` | text | not null |
| `payload` | jsonb | not null, default 'received' |
| `status` | text | not null, default 'received' |
| `error` | text | — |
| `processed_at` | timestamptz | — |
| `createdAt` | now | — |

### settings

Drizzle export: `settings`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid, references workspaces.id, on delete cascade |
| `workspace_id` | uuid | not null, references workspaces.id, on delete cascade |
| `key` | text | not null |
| `value` | jsonb | not null |
| `updatedAt` | updated | — |
| `createdAt` | now | — |

### agents

Drizzle export: `agents`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, not null, default random uuid |
| `key` | text | not null |
| `name` | text | not null, default '' |
| `description` | text | not null, default '' |
| `category` | text | not null, default 'core' |
| `enabled` | boolean | not null, default true |
| `status` | text | not null, default 'idle' |
| `model_tier` | text | not null, default 'standard' |
| `max_daily_runs` | integer | not null, default 500 |
| `max_cost_cents_per_run` | integer | not null, default 25 |
| `timeout_seconds` | integer | not null, default 120 |
| `avg_duration_ms` | integer | not null, default 0 |
| `success_count` | integer | not null, default 0 |
| `failure_count` | integer | not null, default 0 |
| `last_run_at` | timestamptz | — |
| `last_heartbeat_at` | timestamptz | — |
| `last_error` | text | — |
| `createdAt` | now | — |
| `updatedAt` | updated | — |

### agent_runs

Drizzle export: `agentRuns`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid, references workspaces.id, on delete cascade |
| `workspace_id` | uuid | not null, references workspaces.id, on delete cascade |
| `agent_key` | text | not null |
| `job_id` | uuid | not null, default 'system' |
| `triggered_by` | text | not null, default 'system' |
| `status` | text | not null, default 'running' |
| `input` | jsonb | — |
| `output` | jsonb | — |
| `error` | text | not null, default 1 |
| `attempt` | integer | not null, default 1 |
| `provider` | text | — |
| `model` | text | not null, default 0 |
| `tokens_used` | integer | not null, default 0 |
| `cost_cents` | integer | not null, default 0 |
| `duration_ms` | integer | not null, default now() |
| `started_at` | timestamptz | not null, default now() |
| `finished_at` | timestamptz | — |
| `createdAt` | now | — |

### agent_memory

Drizzle export: `agentMemory`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `workspace_id` | uuid | not null |
| `agent_key` | text | not null |
| `kind` | text | not null |
| `key` | text | not null |
| `content` | text | not null, default 3 |
| `importance` | integer | not null, default 3 |
| `ref_type` | text | — |
| `ref_id` | text | — |
| `data` | jsonb | — |
| `expires_at` | timestamptz | — |
| `createdAt` | now | — |

### memory_summaries

Drizzle export: `memorySummaries`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `workspace_id` | uuid | not null |
| `scope` | text | not null |
| `scope_key` | text | not null |
| `period_start` | timestamptz | not null |
| `period_end` | timestamptz | not null |
| `summary` | text | not null |
| `metrics` | jsonb | — |
| `createdAt` | now | — |

### sources

Drizzle export: `sources`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid, references workspaces.id, on delete cascade |
| `workspace_id` | uuid | not null, references workspaces.id, on delete cascade |
| `name` | text | not null, default 'api' |
| `type` | text | not null, default 'api' |
| `url` | text | — |
| `api_endpoint` | text | not null, default 'public_api' |
| `permission_status` | text | not null, default 'public_api' |
| `terms_url` | text | not null, default false |
| `requires_credentials` | boolean | not null, default false |
| `credential_env_var` | text | not null, default 60 |
| `rate_limit_per_hour` | integer | not null, default 60 |
| `reliability_score` | numeric | not null, array, default '50' |
| `categories` | text | not null, array, default [] |
| `enabled` | boolean | not null, default true |
| `last_scan_at` | timestamptz | — |
| `last_scan_status` | text | — |
| `last_error` | text | not null, default 0 |
| `scan_count` | integer | not null, default 0 |
| `discovered_count` | integer | not null, default 0 |
| `config` | jsonb | not null, default {} |
| `createdAt` | now | — |
| `updatedAt` | updated | — |

### opportunities

Drizzle export: `opportunities`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `workspace_id` | uuid | not null |
| `source_id` | uuid | not null, default 'manual', references sources.id, on delete set null |
| `source_name` | text | not null, default 'manual' |
| `title` | text | not null, default '' |
| `description` | text | not null, default '' |
| `url` | text | not null, default 'other' |
| `category` | text | not null, array, default 'other' |
| `tags` | text | not null, array, default [] |
| `region` | text | not null |
| `fingerprint` | text | not null, default 'discovered' |
| `status` | text | not null, default 'discovered' |
| `spam_score` | numeric | not null, default '0' |
| `relevancy_score` | numeric | not null, default '0' |
| `url_valid` | boolean | not null, default true |
| `is_duplicate_of` | uuid | — |
| `raw` | jsonb | not null, default {} |
| `metadata` | jsonb | not null, default now() |
| `discovered_at` | timestamptz | not null, default now() |
| `cleaned_at` | timestamptz | — |
| `expires_at` | timestamptz | — |
| `decision` | text | — |
| `decision_note` | text | references users.id, on delete set null |
| `decided_by_user_id` | uuid | references users.id, on delete set null |
| `decided_at` | timestamptz | not null, default false |
| `demo` | boolean | not null, default false |
| `createdAt` | now | — |
| `updatedAt` | updated | — |
| `deleted_at` | timestamptz | — |

### opportunity_scores

Drizzle export: `opportunityScores`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `opportunity_id` | uuid | not null |
| `workspace_id` | uuid | not null |
| `demand` | integer | not null, default 50 |
| `competition` | integer | not null, default 50 |
| `monetization` | integer | not null, default 50 |
| `startup_cost` | integer | not null, default 50 |
| `operating_cost` | integer | not null, default 50 |
| `automation_potential` | integer | not null, default 50 |
| `scalability` | integer | not null, default 50 |
| `time_to_revenue` | integer | not null, default 50 |
| `difficulty` | integer | not null, default 50 |
| `risk` | integer | not null, default 50 |
| `final_score` | numeric | not null, default 60 |
| `confidence` | integer | not null, default 60 |
| `verdict` | text | not null, default 'consider' |
| `rationale` | jsonb | not null, default {} |
| `summary` | text | not null, default '' |
| `weights` | jsonb | not null, default {} |
| `engine` | text | not null, default 'heuristic' |
| `provider` | text | — |
| `model` | text | not null, default 1 |
| `version` | integer | not null, default 1 |
| `createdAt` | now | — |

### strategies

Drizzle export: `strategies`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `workspace_id` | uuid | not null |
| `opportunity_id` | uuid | not null |
| `version` | integer | not null, default 1 |
| `status` | text | not null, default 'draft' |
| `title` | text | not null, default '' |
| `problem` | text | not null, default '' |
| `target_customer` | text | not null, default '' |
| `solution` | text | not null, default '' |
| `business_model` | text | not null, default '' |
| `monetization` | text | not null, default '' |
| `acquisition` | text | not null, default '' |
| `operating_model` | text | not null, default '' |
| `cost_estimate` | jsonb | — |
| `scenarios` | jsonb | not null, default [] |
| `risks` | jsonb | not null, default [] |
| `tech_requirements` | jsonb | not null, default [] |
| `launch_plan` | jsonb | not null, default [] |
| `success_metrics` | jsonb | not null, default [] |
| `engine` | text | not null, default 'heuristic' |
| `provider` | text | — |
| `model` | text | — |
| `prompt_version` | text | — |
| `createdAt` | now | — |
| `updatedAt` | updated | — |

### projects

Drizzle export: `projects`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `workspace_id` | uuid | not null |
| `opportunity_id` | uuid | references opportunities.id, on delete set null |
| `strategy_id` | uuid | not null, references strategies.id, on delete set null |
| `name` | text | not null |
| `slug` | text | not null, default '' |
| `objective` | text | not null, default '' |
| `status` | text | not null, default 'DISCOVERED' |
| `business_model` | text | not null, default '' |
| `revenue_model` | text | not null, default '' |
| `cost_model` | jsonb | not null, default 'approval_required' |
| `automation_level` | text | not null, default 'approval_required' |
| `budget_cents` | integer | not null, default 0 |
| `spent_cents` | integer | not null, default 0 |
| `revenue_cents` | integer | not null, default 0 |
| `profit_cents` | integer | not null, default 0 |
| `progress` | integer | not null, default 0 |
| `metrics` | jsonb | not null, default {} |
| `launched_at` | timestamptz | — |
| `paused_at` | timestamptz | — |
| `completed_at` | timestamptz | — |
| `failure_reason` | text | not null, default false |
| `demo` | boolean | not null, default false |
| `createdAt` | now | — |
| `updatedAt` | updated | — |
| `deleted_at` | timestamptz | — |

### tasks

Drizzle export: `tasks`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `workspace_id` | uuid | not null |
| `project_id` | uuid | not null |
| `parent_id` | uuid | not null |
| `title` | text | not null, default '' |
| `description` | text | not null, default '' |
| `status` | text | not null, default 'todo' |
| `priority` | integer | not null, default 3 |
| `assignee_agent_key` | text | not null, array, default [] |
| `depends_on` | uuid | not null, array, default [] |
| `position` | integer | not null, default 0 |
| `requires_approval` | boolean | not null, default false |
| `estimated_cost_cents` | integer | not null, default 0 |
| `actual_cost_cents` | integer | not null, default 0 |
| `due_at` | timestamptz | — |
| `started_at` | timestamptz | — |
| `finished_at` | timestamptz | not null, default 0 |
| `attempts` | integer | not null, default 0 |
| `last_error` | text | — |
| `result` | jsonb | — |
| `createdAt` | now | — |
| `updatedAt` | updated | — |

### project_events

Drizzle export: `projectEvents`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `workspace_id` | uuid | not null |
| `project_id` | uuid | not null, references projects.id, on delete cascade |
| `type` | text | not null |
| `message` | text | not null, default 'system' |
| `actor` | text | not null, default 'system' |
| `data` | jsonb | — |
| `createdAt` | now | — |

### workflows

Drizzle export: `workflows`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `workspace_id` | uuid | not null |
| `name` | text | not null, default '' |
| `description` | text | not null, default '' |
| `definition` | jsonb | not null |
| `schedule` | text | not null, default 'UTC' |
| `schedule_timezone` | text | not null, default 'UTC' |
| `status` | text | not null, default 'active' |
| `enabled` | boolean | not null, default true |
| `is_template` | boolean | not null, default false |
| `next_run_at` | timestamptz | — |
| `last_run_at` | timestamptz | — |
| `last_status` | text | not null, default 0 |
| `run_count` | integer | not null, default 0 |
| `createdAt` | now | — |
| `updatedAt` | updated | — |

### workflow_runs

Drizzle export: `workflowRuns`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `workspace_id` | uuid | not null |
| `workflow_id` | uuid | not null |
| `trigger` | text | not null, default 'manual' |
| `status` | text | not null, default 'running' |
| `steps` | jsonb | not null, default [] |
| `context_data` | jsonb | not null, default {} |
| `error` | text | not null, default 1 |
| `attempt` | integer | not null, default now() |
| `started_at` | timestamptz | not null, default now() |
| `finished_at` | timestamptz | not null, default 0 |
| `duration_ms` | integer | not null, default 0 |
| `createdAt` | now | — |

### approvals

Drizzle export: `approvals`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `workspace_id` | uuid | not null |
| `project_id` | uuid | references projects.id, on delete cascade |
| `opportunity_id` | uuid | references opportunities.id, on delete set null |
| `workflow_run_id` | uuid | not null, references workflow_runs.id, on delete set null |
| `action_type` | text | not null |
| `title` | text | not null, default '' |
| `reason` | text | not null, default '' |
| `expected_cost_cents` | integer | not null, default 0 |
| `potential_benefit` | text | not null, default '' |
| `risk` | text | not null, default 'medium' |
| `payload` | jsonb | not null, default {} |
| `status` | text | not null, default 'pending' |
| `requested_by_agent` | text | not null, default 'system', references users.id, on delete set null |
| `decided_by_user_id` | uuid | references users.id, on delete set null |
| `decision_note` | text | — |
| `decided_at` | timestamptz | — |
| `defer_until` | timestamptz | — |
| `expires_at` | timestamptz | — |
| `executed_at` | timestamptz | — |
| `execution_result` | jsonb | — |
| `execution_error` | text | — |
| `createdAt` | now | — |
| `updatedAt` | updated | — |

### content_assets

Drizzle export: `contentAssets`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `workspace_id` | uuid | not null |
| `project_id` | uuid | references projects.id, on delete cascade |
| `opportunity_id` | uuid | not null, references opportunities.id, on delete set null |
| `type` | text | not null |
| `title` | text | not null, default '' |
| `slug` | text | not null, default '' |
| `summary` | text | not null, default '' |
| `body` | text | not null, default '' |
| `format` | text | not null, default 'markdown' |
| `metadata` | jsonb | not null, default {} |
| `version` | integer | not null, default 1 |
| `parent_version_id` | uuid | not null, default 'draft' |
| `status` | text | not null, default 'draft' |
| `published_url` | text | — |
| `published_at` | timestamptz | not null, default 'heuristic' |
| `engine` | text | not null, default 'heuristic' |
| `provider` | text | — |
| `model` | text | — |
| `storage_key` | text | not null, default false |
| `demo` | boolean | not null, default false |
| `createdAt` | now | — |
| `updatedAt` | updated | — |
| `deleted_at` | timestamptz | — |

### revenue_transactions

Drizzle export: `revenueTransactions`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `workspace_id` | uuid | not null |
| `project_id` | uuid | not null, default 'manual', references projects.id, on delete set null |
| `source` | text | not null, default 'manual' |
| `provider` | text | — |
| `provider_transaction_id` | text | not null, default '' |
| `description` | text | not null, default '' |
| `gross_cents` | integer | not null, default 0 |
| `fee_cents` | integer | not null, default 0 |
| `net_cents` | integer | not null, default 0 |
| `currency` | text | not null, default 'USD' |
| `status` | text | not null, default 'confirmed' |
| `verification` | text | not null, default now() |
| `occurred_at` | timestamptz | not null, default now(), references users.id, on delete set null |
| `recorded_by_user_id` | uuid | not null, default {}, references users.id, on delete set null |
| `metadata` | jsonb | not null, default {} |
| `demo` | boolean | not null, default false |
| `createdAt` | now | — |
| `updatedAt` | updated | — |

### expenses

Drizzle export: `expenses`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `workspace_id` | uuid | not null |
| `project_id` | uuid | references projects.id, on delete set null |
| `agent_run_id` | uuid | not null, default 'other', references agent_runs.id, on delete set null |
| `category` | text | not null, default 'other' |
| `description` | text | not null, default '' |
| `amount_cents` | integer | not null, default 0 |
| `currency` | text | not null, default 'USD' |
| `provider` | text | not null, default 'manual' |
| `verification` | text | not null, default now() |
| `occurred_at` | timestamptz | not null, default now(), references users.id, on delete set null |
| `recorded_by_user_id` | uuid | not null, default {}, references users.id, on delete set null |
| `metadata` | jsonb | not null, default {} |
| `demo` | boolean | not null, default false |
| `createdAt` | now | — |
| `updatedAt` | updated | — |

### budgets

Drizzle export: `budgets`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `workspace_id` | uuid | not null |
| `scope` | text | not null, default 'workspace' |
| `scope_key` | text | not null, default 'monthly' |
| `period` | text | not null, default 'monthly' |
| `limit_cents` | integer | not null, default 0 |
| `alert_threshold_pct` | integer | not null, default 80 |
| `enabled` | boolean | not null, default true |
| `createdAt` | now | — |
| `updatedAt` | updated | — |

### budget_ledger

Drizzle export: `budgetLedger`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `workspace_id` | uuid | not null |
| `scope` | text | not null, default 'workspace' |
| `scope_key` | text | not null, default 'debit' |
| `direction` | text | not null, default 'debit' |
| `amount_cents` | integer | not null, default 0 |
| `currency` | text | not null, default 'USD' |
| `reason` | text | not null, default '' |
| `ref_type` | text | — |
| `ref_id` | text | references projects.id, on delete set null |
| `project_id` | uuid | not null, default now(), references projects.id, on delete set null |
| `occurred_at` | timestamptz | not null, default now() |
| `createdAt` | now | — |

### analytics_events

Drizzle export: `analyticsEvents`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid, references workspaces.id, on delete cascade |
| `workspace_id` | uuid | references workspaces.id, on delete cascade |
| `project_id` | uuid | not null, references projects.id, on delete set null |
| `type` | text | not null, default '' |
| `name` | text | not null, default '' |
| `value` | numeric | not null, default '0' |
| `properties` | jsonb | not null, default {} |
| `visitor_id` | text | — |
| `url` | text | not null, default false |
| `demo` | boolean | not null, default now() |
| `occurred_at` | timestamptz | not null, default now() |
| `createdAt` | now | — |

### notifications

Drizzle export: `notifications`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `workspace_id` | uuid | not null |
| `user_id` | uuid | not null, references users.id, on delete cascade |
| `type` | text | not null, default 'info' |
| `severity` | text | not null, default 'info' |
| `title` | text | not null, default '' |
| `body` | text | not null, default '' |
| `link` | text | not null, array, default [] |
| `channels` | text | not null, array, default [] |
| `status` | text | not null, default 'unread' |
| `delivery` | jsonb | not null, default {} |
| `dedupe_key` | text | — |
| `read_at` | timestamptz | — |
| `createdAt` | now | — |

### alerts

Drizzle export: `alerts`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid, references workspaces.id, on delete cascade |
| `workspace_id` | uuid | not null, references workspaces.id, on delete cascade |
| `type` | text | not null, default 'warning' |
| `severity` | text | not null, default 'warning' |
| `title` | text | not null, default '' |
| `message` | text | not null, default '' |
| `source` | text | not null, default 'system' |
| `status` | text | not null, default 'open' |
| `occurrence_count` | integer | not null, default 1, references users.id, on delete set null |
| `acknowledged_by` | uuid | references users.id, on delete set null |
| `resolved_at` | timestamptz | not null, default {} |
| `metadata` | jsonb | not null, default now() |
| `first_seen_at` | timestamptz | not null, default now() |
| `last_seen_at` | timestamptz | not null, default now() |
| `createdAt` | now | — |

### api_usage

Drizzle export: `apiUsage`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid, references workspaces.id, on delete cascade |
| `workspace_id` | uuid | references workspaces.id, on delete cascade |
| `user_id` | uuid | references users.id, on delete set null |
| `agent_run_id` | uuid | not null, references agent_runs.id, on delete set null |
| `provider` | text | not null |
| `model` | text | not null, default 'generate' |
| `operation` | text | not null, default 'generate' |
| `prompt_tokens` | integer | not null, default 0 |
| `completion_tokens` | integer | not null, default 0 |
| `total_tokens` | integer | not null, default 0 |
| `cost_cents` | numeric | not null, default '0' |
| `endpoint` | text | not null, default 0 |
| `latency_ms` | integer | not null, default 0 |
| `status` | text | not null, default 'ok' |
| `error` | text | — |
| `createdAt` | now | — |

### system_logs

Drizzle export: `systemLogs`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid, references workspaces.id, on delete cascade |
| `workspace_id` | uuid | not null, default 'info', references workspaces.id, on delete cascade |
| `level` | text | not null, default 'info' |
| `source` | text | not null, default 'app' |
| `message` | text | not null |
| `context` | jsonb | — |
| `request_id` | text | references users.id, on delete set null |
| `user_id` | uuid | references users.id, on delete set null |
| `duration_ms` | integer | — |
| `createdAt` | now | — |

### jobs

Drizzle export: `jobs`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, not null, default random uuid |
| `queue` | text | not null, default 'default' |
| `name` | text | not null, default {} |
| `payload` | jsonb | not null, default {} |
| `status` | text | not null, default 'queued' |
| `priority` | integer | not null, default 5 |
| `attempts` | integer | not null, default 0 |
| `max_attempts` | integer | not null, default 3 |
| `timeout_ms` | integer | not null, default now() |
| `run_at` | timestamptz | not null, default now() |
| `started_at` | timestamptz | — |
| `finished_at` | timestamptz | — |
| `duration_ms` | integer | — |
| `locked_by` | text | — |
| `locked_at` | timestamptz | — |
| `last_error` | text | — |
| `stack` | text | references workspaces.id, on delete cascade |
| `workspace_id` | uuid | references workspaces.id, on delete cascade |
| `dedupe_key` | text | — |
| `result` | jsonb | — |
| `createdAt` | now | — |
| `updatedAt` | updated | — |

### schedules

Drizzle export: `schedules`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, not null, default random uuid |
| `key` | text | not null |
| `name` | text | not null |
| `cron` | text | not null, default 'UTC' |
| `timezone` | text | not null, default 'UTC' |
| `job_name` | text | not null, default {} |
| `payload` | jsonb | not null, default {}, references workspaces.id, on delete cascade |
| `workspace_id` | uuid | not null, default true, references workspaces.id, on delete cascade |
| `enabled` | boolean | not null, default true |
| `system` | boolean | not null, default true |
| `next_run_at` | timestamptz | — |
| `last_run_at` | timestamptz | — |
| `last_status` | text | — |
| `last_error` | text | not null, default 0 |
| `run_count` | integer | not null, default 0 |
| `createdAt` | now | — |
| `updatedAt` | updated | — |

### worker_heartbeats

Drizzle export: `workerHeartbeats`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, not null, default random uuid |
| `worker_id` | text | not null, default 'worker' |
| `role` | text | not null, default 'worker' |
| `hostname` | text | not null, default 'unknown' |
| `version` | text | not null, default '1.0.0' |
| `status` | text | not null, default 'online' |
| `processed_count` | integer | not null, default 0 |
| `error_count` | integer | not null, default 0 |
| `metadata` | jsonb | not null, default now() |
| `started_at` | timestamptz | not null, default now() |
| `last_heartbeat_at` | timestamptz | not null, default now() |

### reports

Drizzle export: `reports`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `workspace_id` | uuid | not null |
| `type` | text | not null, default 'daily' |
| `period_start` | timestamptz | not null |
| `period_end` | timestamptz | not null, default '' |
| `summary` | text | not null, default '' |
| `data` | jsonb | not null, default {} |
| `recommendations` | jsonb | not null, default [] |
| `status` | text | not null, default 'ready' |
| `demo` | boolean | not null, default false |
| `createdAt` | now | — |

### email_outbox

Drizzle export: `emailOutbox`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, not null, default random uuid |
| `to` | text | not null |
| `subject` | text | not null |
| `body` | text | not null |
| `html` | text | not null, default 'console' |
| `provider` | text | not null, default 'console' |
| `status` | text | not null, default 'queued' |
| `attempts` | integer | not null, default 0 |
| `error` | text | — |
| `sent_at` | timestamptz | — |
| `createdAt` | now | — |

### storage_objects

Drizzle export: `storageObjects`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid, references workspaces.id, on delete cascade |
| `workspace_id` | uuid | not null, default 'local', references workspaces.id, on delete cascade |
| `provider` | text | not null, default 'local' |
| `bucket` | text | not null, default 'aiba-assets' |
| `key` | text | not null, default 'application/octet-stream' |
| `content_type` | text | not null, default 'application/octet-stream' |
| `size_bytes` | integer | not null, default 0 |
| `checksum_sha256` | text | — |
| `url` | text | not null, default false |
| `is_public` | boolean | not null, default false |
| `created_by_agent` | text | — |
| `createdAt` | now | — |

### opportunity_decisions

Drizzle export: `opportunityCandidates`

| Column | Type | Attributes |
| --- | --- | --- |
| `id` | uuid | primary key, default random uuid |
| `workspace_id` | uuid | not null |
| `opportunity_id` | uuid | not null |
| `decision` | text | not null, default '' |
| `reason` | text | not null, default '' |
| `actor` | text | not null, default 'user' |
| `score_at_decision` | numeric | — |
| `createdAt` | now | — |

